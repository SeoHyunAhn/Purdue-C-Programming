Step (1/3):

Complete the intMatches function which takes an int key and an int value and checks if they are equal. If they are equal return 1 else return 0

Step (2/3):

Complete the studentMatchesById function which takes an int key and a student struct and checks if the student's id field matches the key. If they are equal return 1 else return 0


Step (3/3):

Complete the findElement function which takes an array of void *s and a pointer to a return value which is filled set to the array element with the key matching the key passed to findElement. Use the matches function
to determine if an element matches the given key.
